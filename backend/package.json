{
  "name": "wbd-backend",
  "version": "1.0.0",
  "main": "src/index.tss",
  "type": "module",
  "scripts": {
    "docker:dev": "npm run db:migrate && npm run db:generate && npm run dev",
    "dev": "tsx watch src/index.ts --poll --watch-files src",
    "start": "tsx src/index.ts",
    "db:pull": "prisma db pull",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy",
    "db:generate": "prisma generate"
  },
  "author": "WBD",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@prisma/client": "^5.22.0",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "hono": "^4.6.11"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/jsonwebtoken": "^9.0.7",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
